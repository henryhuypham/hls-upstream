# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Test-Stream-Audio' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Alamofire', '~> 4.4'
  pod 'ObjectMapper'
  pod 'SwiftyJSON'
  pod 'RxSwift',    '~> 3.0'
  pod 'RxCocoa',    '~> 3.0'

  # Pods for Test-Stream-Audio

  target 'Test-Stream-AudioTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Test-Stream-AudioUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
